/**
 * Plain constants for the valuation config (NOT a 'use server' module, so a
 * non-function export is allowed and it can be imported by both server actions
 * and server components / crons).
 */
export const VALUATION_CONFIG_KEY = 'valuation.config';
