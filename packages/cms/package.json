{
  "name": "@repo/cms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'cms package stubbed — basehub disabled' && exit 0",
    "build": "echo 'cms package stubbed — basehub disabled' && exit 0",
    "analyze": "echo 'cms package stubbed — basehub disabled' && exit 0",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.12.0",
    "basehub": "^8.1.18",
    "react": "^19.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "22.13.9",
    "@types/react": "19.0.10",
    "@types/react-dom": "^19.0.4",
    "next": "15.5.15"
  }
}
